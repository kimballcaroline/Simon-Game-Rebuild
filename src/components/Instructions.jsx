const Instructions = () => {
  return (
    <div className='bg-white text-neutral-800 p-4 m-4 rounded-md w-11/12 self-center text-center'>
      <div>Instructions:</div>
      <div>
        Click the buttons in the order that they flash to move on to the next
        round. Each round will be one flash longer than the last. <br />
        Each round of flashes will only show once, so pay attention!
      </div>
    </div>
  );
};

export default Instructions;
